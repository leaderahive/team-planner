import React from "react";
import ReactDOM from "react-dom/client";
import TeamPlanner from "./TeamPlanner.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TeamPlanner />
  </React.StrictMode>
);
